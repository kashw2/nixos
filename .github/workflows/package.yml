name: Build Package
on:
  workflow_dispatch:
  pull_request:
    paths:
      - flake.nix
      - flake.lock
      - nix/packages/nushell.nix
      - nix/packages/kitty.nix
      - nix/packages/tmux.nix
      - nix/packages/fastfetch/**
      - nix/packages/act.nix
      - nix/packages/floci-cli.nix
      - nix/packages/desktop-environment/sddm-sugar-candy.nix
      - nix/packages/desktop-environment/hyprland.nix
      - nix/packages/desktop-environment/hypridle.nix
      - nix/packages/desktop-environment/hyprlock.nix
      - nix/packages/desktop-environment/hyprpaper.nix
      - nix/packages/desktop-environment/quickshell.nix
      - nix/packages/desktop-environment/quickshell/**
      - nix/deploy.nix
      - nix/packages/nixvim/**
      - nix/modules/nixvim/**
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      packages: ${{ steps.classify.outputs.packages }}
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: dorny/paths-filter@v4.0.3
        id: changes
        if: github.event_name == 'pull_request'
        with:
          filters: |
            flake:
              - 'flake.nix'
              - 'flake.lock'
            nushell: 'nix/packages/nushell.nix'
            kitty: 'nix/packages/kitty.nix'
            tmux: 'nix/packages/tmux.nix'
            fastfetch: 'nix/packages/fastfetch/**'
            act: 'nix/packages/act.nix'
            floci-cli: 'nix/packages/floci-cli.nix'
            sddm-sugar-candy: 'nix/packages/desktop-environment/sddm-sugar-candy.nix'
            hyprland: 'nix/packages/desktop-environment/hyprland.nix'
            hypridle: 'nix/packages/desktop-environment/hypridle.nix'
            hyprlock: 'nix/packages/desktop-environment/hyprlock.nix'
            hyprpaper: 'nix/packages/desktop-environment/hyprpaper.nix'
            atone-nvim: 'nix/packages/nixvim/atone-nvim.nix'
            codestats: 'nix/packages/nixvim/codestats.nix'
            bruno-nvim: 'nix/packages/nixvim/tree-sitter-bruno.nix'
            tree-sitter-bruno: 'nix/packages/nixvim/tree-sitter-bruno.nix'
            neovim:
              - 'nix/packages/nixvim/**'
              - 'nix/modules/nixvim/**'
            quickshell:
              - 'nix/packages/desktop-environment/quickshell.nix'
              - 'nix/packages/desktop-environment/quickshell/**'
            deploy-host: 'nix/deploy.nix'
      - id: classify
        env:
          EVENT: ${{ github.event_name }}
          FLAKE_CHANGED: ${{ steps.changes.outputs.flake }}
          CHANGES: ${{ steps.changes.outputs.changes }}
        run: |
          ALL='["nushell","act","floci-cli","kitty","tmux","sddm-sugar-candy","hyprland","hypridle","hyprlock","hyprpaper","atone-nvim","codestats","bruno-nvim","tree-sitter-bruno","neovim","quickshell","fastfetch","deploy-host"]'
          if [ "$EVENT" = "workflow_dispatch" ] || [ "$FLAKE_CHANGED" = "true" ]; then
            echo "packages=$ALL" >> "$GITHUB_OUTPUT"
          else
            echo "packages=$(jq -c 'map(select(. != "flake"))' <<< "$CHANGES")" >> "$GITHUB_OUTPUT"
          fi
  build:
    name: Build ${{ matrix.package }}
    needs: detect-changes
    if: needs.detect-changes.outputs.packages != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        package: ${{ fromJSON(needs.detect-changes.outputs.packages) }}
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: cachix/install-nix-action@v31.11.1
        with:
          extra_nix_config: |
            extra-substituters = https://nix-community.cachix.org
            extra-trusted-public-keys = nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs=
      - uses: tailscale/github-action@v4.2.0
        continue-on-error: true
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci
      - uses: ryanccn/attic-action@v0
        continue-on-error: true
        with:
          endpoint: http://100.116.38.8:8080
          cache: nixos
          token: ${{ secrets.ATTIC_TOKEN }}
      - run: nix develop --command nix build .#${{ matrix.package }} --impure
        env:
          NIXPKGS_ALLOW_UNFREE: 1
