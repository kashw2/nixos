name: Build Package
on:
  workflow_dispatch:
  pull_request:
    paths:
      - flake.nix
      - flake.lock
      - nix/features/nushell.nix
      - nix/features/kitty.nix
      - nix/features/tmux.nix
      - nix/features/fastfetch/**
      - nix/features/act.nix
      - nix/features/floci-cli.nix
      - nix/features/desktop-environment/sddm-sugar-candy.nix
      - nix/features/desktop-environment/hyprland.nix
      - nix/features/desktop-environment/hypridle.nix
      - nix/features/desktop-environment/hyprlock.nix
      - nix/features/desktop-environment/hyprpaper.nix
      - nix/features/desktop-environment/quickshell.nix
      - nix/features/desktop-environment/quickshell/**
      - nix/deploy.nix
      - nix/features/nixvim/**
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      packages: ${{ steps.classify.outputs.packages }}
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: dorny/paths-filter@v4.0.3
        id: changes
        if: github.event_name == 'pull_request'
        with:
          filters: |
            flake:
              - 'flake.nix'
              - 'flake.lock'
            nushell: 'nix/features/nushell.nix'
            kitty: 'nix/features/kitty.nix'
            tmux: 'nix/features/tmux.nix'
            fastfetch: 'nix/features/fastfetch/**'
            act: 'nix/features/act.nix'
            floci-cli: 'nix/features/floci-cli.nix'
            sddm-sugar-candy: 'nix/features/desktop-environment/sddm-sugar-candy.nix'
            hyprland: 'nix/features/desktop-environment/hyprland.nix'
            hypridle: 'nix/features/desktop-environment/hypridle.nix'
            hyprlock: 'nix/features/desktop-environment/hyprlock.nix'
            hyprpaper: 'nix/features/desktop-environment/hyprpaper.nix'
            atone-nvim: 'nix/features/nixvim/atone-nvim.nix'
            codestats: 'nix/features/nixvim/codestats.nix'
            bruno-nvim: 'nix/features/nixvim/tree-sitter-bruno.nix'
            tree-sitter-bruno: 'nix/features/nixvim/tree-sitter-bruno.nix'
            neovim: 'nix/features/nixvim/**'
            quickshell:
              - 'nix/features/desktop-environment/quickshell.nix'
              - 'nix/features/desktop-environment/quickshell/**'
            deploy-host: 'nix/deploy.nix'
      - id: classify
        env:
          EVENT: ${{ github.event_name }}
          FLAKE_CHANGED: ${{ steps.changes.outputs.flake }}
          CHANGES: ${{ steps.changes.outputs.changes }}
        run: |
          ALL='["nushell","act","floci-cli","kitty","tmux","sddm-sugar-candy","hyprland","hypridle","hyprlock","hyprpaper","atone-nvim","codestats","bruno-nvim","tree-sitter-bruno","neovim","quickshell","fastfetch","deploy-host"]'
          if [ "$EVENT" = "workflow_dispatch" ] || [ "$FLAKE_CHANGED" = "true" ]; then
            echo "packages=$ALL" >> "$GITHUB_OUTPUT"
          else
            echo "packages=$(jq -c 'map(select(. != "flake"))' <<< "$CHANGES")" >> "$GITHUB_OUTPUT"
          fi
  build:
    name: Build ${{ matrix.package }}
    needs: detect-changes
    if: needs.detect-changes.outputs.packages != '[]'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        package: ${{ fromJSON(needs.detect-changes.outputs.packages) }}
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: cachix/install-nix-action@v31.11.1
        with:
          extra_nix_config: |
            extra-substituters = https://nix-community.cachix.org
            extra-trusted-public-keys = nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs=
      - uses: tailscale/github-action@v4.2.0
        continue-on-error: true
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci
      - uses: ryanccn/attic-action@v0
        continue-on-error: true
        with:
          endpoint: http://100.116.38.8:8080
          cache: nixos
          token: ${{ secrets.ATTIC_TOKEN }}
      - run: nix develop --command nix build .#${{ matrix.package }} --impure
        env:
          NIXPKGS_ALLOW_UNFREE: 1
